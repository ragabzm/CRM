<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
        <testsuite name="Architecture">
            <directory>tests/Architecture</directory>
        </testsuite>
    </testsuites>

    <groups>
        <!--
            Excluded by default. Seeding fifty thousand rows and timing five
            queries takes about three minutes, which is worth running
            deliberately rather than on every save.

            Run it with: php artisan test (space) (dash)(dash)group=performance
        -->
        <exclude>
            <group>performance</group>
        </exclude>
    </groups>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <!--
            The suite outgrew PHP's 128M default at around 800 tests: each
            case boots the application and RefreshDatabase keeps a schema in
            memory, and the total crept past the limit mid-run. The symptom is
            a fatal error in whichever test happens to be running, which reads
            like a bug in that test rather than a limit being reached.
        -->
        <ini name="memory_limit" value="512M"/>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="DB_URL" value=""/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
        <env name="PROBLEM_TYPE_BASE_URI" value="https://errors.ragab-crm"/>
        <!-- The json channel writes to stdout; keep it out of the test report.
             LoggingTest targets the channel explicitly instead. -->
        <env name="LOG_CHANNEL" value="single"/>
        <!-- The audit channel writes to stdout by design; point it at a file in
             tests so audit records do not interleave with the test report.
             LogsAuditTrail asserts against this stream where it matters. -->
        <env name="LOG_AUDIT_STREAM" value="php://memory"/>
    </php>
</phpunit>
