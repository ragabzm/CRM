# Story 495 — outstanding manual verification

Three Done Criteria cannot be closed from a terminal. They need a person with
a phone and a keyboard, per the plan's own "Manual — real devices" step (step 7
of the Verification Steps section).

## Not verified: real-device sweep

- Criterion 4 — every function completable at 390 px in a **mobile browser**,
  keyboard-only, with a visible `--focus-ring` indicator.
- Criterion 5 — `FileInput` opening the **camera and photo library** on a real
  iOS Safari and a real Android Chrome.
- Criterion 8 — no action reachable only by hover, by **manual keyboard
  traversal** at 1440 px.

What IS verified programmatically: `accept` and `capture` are exposed by
`frontend/components/ui/file-input.tsx`; `--focus-ring-*` tokens exist in
`tokens.css`; axe runs the full WCAG 2.1 A/AA set (nothing disabled but
`region`, and only for component fixtures) over every page in `ltr` and `rtl`
via `pnpm test:a11y`, which includes `interactive-supports-focus`.

Close these three boxes only after someone actually runs the sweep on hardware.
